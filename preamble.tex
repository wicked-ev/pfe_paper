% Encoding
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}

% Math
\usepackage{amsmath, amssymb}

% Graphics
\usepackage{graphicx}
\graphicspath{{figures/}}

% Tables
\usepackage{booktabs}

% References (ONLY use natbib with numbers for the 'plain' style)
\usepackage[numbers]{natbib} 

% Hyperlinks
\usepackage{hyperref}

% Better spacing
\usepackage{setspace}

% Custom commands
\newcommand{\todo}[1]{\textcolor{red}{TODO: #1}}
